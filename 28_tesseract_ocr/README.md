# Tesseract OCR Mini Project
WHY: OCR converts text visible in images into machine-readable text.
You must install the Tesseract OCR engine separately, then install Python packages.
Put a text image named `text.png` in this folder.
